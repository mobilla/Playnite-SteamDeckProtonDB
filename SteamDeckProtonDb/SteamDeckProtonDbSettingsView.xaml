<UserControl x:Class="SteamDeckProtonDb.SteamDeckProtonDbSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <Grid Margin="12">
        <TabControl>
            <!-- Metadata Tab -->
            <TabItem Header="Metadata">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="12,12,12,12">
                        <!-- Steam Deck Settings -->
                        <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_SteamDeckMetadata}" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>
                        <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_SteamDeckMetadataDesc}" FontSize="11" Foreground="Gray" Margin="0,0,0,8"/>
                        <CheckBox IsChecked="{Binding EnableSteamDeckSync}" Margin="0,0,0,16">
                            <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_EnableSteamDeckSync}"/>
                        </CheckBox>
                        
                        <!-- ProtonDB Settings -->
                        <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_ProtonDbMetadata}" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>
                        <CheckBox IsChecked="{Binding EnableProtonDbSync}" Margin="0,0,0,8">
                            <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_EnableProtonDbSync}"/>
                        </CheckBox>
                        <CheckBox IsChecked="{Binding EnableProtonDbLink}" Margin="0,0,0,8">
                            <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_EnableProtonDbLink}"/>
                        </CheckBox>
                        <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_ProtonDbSectionDesc}" FontSize="11" Foreground="Gray" Margin="0,0,0,16"/>
                        
                        <!-- Auto-Fetch Settings -->
                        <TextBlock Text="Automation" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>
                        <CheckBox IsChecked="{Binding AutoFetchOnLibraryUpdate}" Margin="0,0,0,8">
                            <TextBlock Text="Auto-fetch status when games are added to library"/>
                        </CheckBox>
                        <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_AutoFetchDesc}" FontSize="11" Foreground="Gray"/>

                        <!-- Non-Steam Matching -->
                        <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_NonSteamMatchingHeader}" FontWeight="Bold" FontSize="14" Margin="0,16,0,8"/>
                        <CheckBox IsChecked="{Binding TryNonSteamMatching}" Margin="0,0,0,4">
                            <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_TryNonSteamMatching}"/>
                        </CheckBox>
                        <CheckBox IsChecked="{Binding AllowFuzzyNonSteamMatching}" Margin="20,0,0,8">
                            <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_AllowFuzzyNonSteamMatching}"/>
                        </CheckBox>
                        <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_NonSteamMatchingDesc}" FontSize="11" Foreground="Gray" Margin="20,0,0,0"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Tags & Features Tab -->
            <TabItem Header="Tags &amp; Features">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="12,12,12,12">
                        <!-- Tags Settings -->
                        <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_TagsHeader}" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>
                        <CheckBox IsChecked="{Binding EnableTags}" Margin="0,0,0,8">
                            <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_EnableTags}"/>
                        </CheckBox>
                        <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_TagsDesc}" FontSize="11" Foreground="Gray" Margin="0,0,0,16"/>
                        
                        <!-- Features Settings -->
                        <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_FeaturesHeader}" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>
                        <CheckBox IsChecked="{Binding EnableFeatures}" Margin="0,0,0,8">
                            <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_EnableFeatures}"/>
                        </CheckBox>
                        <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_FeaturesDesc}" FontSize="11" Foreground="Gray"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Names Tab -->
            <TabItem Header="{DynamicResource LOC_SteamDeckProtonDb_TagFeatureNamesHeader}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="12,12,12,12">
                        <!-- Steam Deck Names -->
                        <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_SteamDeckNamesHeader}" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>
                        <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_SteamDeckNamesDesc}" FontSize="11" Foreground="Gray" Margin="0,0,0,8"/>
                        
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_TagPrefix}" VerticalAlignment="Center" Width="120" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding SteamDeckTagPrefix}" Width="200"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_VerifiedTag}" VerticalAlignment="Center" Width="120" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding SteamDeckVerifiedTag}" Width="200"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_PlayableTag}" VerticalAlignment="Center" Width="120" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding SteamDeckPlayableTag}" Width="200"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                            <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_UnsupportedTag}" VerticalAlignment="Center" Width="120" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding SteamDeckUnsupportedTag}" Width="200"/>
                        </StackPanel>
                        
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_VerifiedFeature}" VerticalAlignment="Center" Width="120" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding SteamDeckVerifiedFeature}" Width="200"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_PlayableFeature}" VerticalAlignment="Center" Width="120" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding SteamDeckPlayableFeature}" Width="200"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                            <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_UnsupportedFeature}" VerticalAlignment="Center" Width="120" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding SteamDeckUnsupportedFeature}" Width="200"/>
                        </StackPanel>

                        <!-- ProtonDB Names -->
                        <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_ProtonDbNamesHeader}" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>
                        <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_ProtonDbNamesDesc}" FontSize="11" Foreground="Gray" Margin="0,0,0,8"/>
                        
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_TagPrefix}" VerticalAlignment="Center" Width="120" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding ProtonDbTagPrefix}" Width="200"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_FeaturePrefix}" VerticalAlignment="Center" Width="120" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding ProtonDbFeaturePrefix}" Width="200"/>
                        </StackPanel>
                        
                        <!-- ProtonDB Rating Names -->
                        <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_ProtonDbRatingNamesHeader}" FontWeight="Bold" FontSize="12" Margin="0,8,0,8"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_PlatinumRating}" VerticalAlignment="Center" Width="120" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding ProtonDbPlatinumRating}" Width="200"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_GoldRating}" VerticalAlignment="Center" Width="120" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding ProtonDbGoldRating}" Width="200"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_SilverRating}" VerticalAlignment="Center" Width="120" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding ProtonDbSilverRating}" Width="200"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_BronzeRating}" VerticalAlignment="Center" Width="120" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding ProtonDbBronzeRating}" Width="200"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_PlausibleRating}" VerticalAlignment="Center" Width="120" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding ProtonDbPlausibleRating}" Width="200"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_BorkedRating}" VerticalAlignment="Center" Width="120" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding ProtonDbBorkedRating}" Width="200"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Cache Tab -->
            <TabItem Header="Cache">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="12,12,12,12">
                        <!-- Cache Settings -->
                        <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_CacheSettings}" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>
                        <CheckBox IsChecked="{Binding UseFileCache}" Margin="0,0,0,8">
                            <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_UseFileCacheLabel}"/>
                        </CheckBox>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_CacheTtl}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding CacheTtlMinutes}" Width="100"/>
                        </StackPanel>
                        <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_CacheTtlDesc}" FontSize="11" Foreground="Gray" Margin="0,0,0,16"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                            <Button x:Name="OpenCacheDirectoryButton" Content="Open Cache Directory" Margin="0,0,10,0" Padding="10,5"/>
                            <Button x:Name="ClearCacheButton" Content="Clear Cache" Margin="0,0,0,0" Padding="10,5"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Advanced Tab -->
            <TabItem Header="Advanced">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="12,12,12,12">
                        <!-- Rate Limiting Settings -->
                        <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_RateLimiting}" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_ProtonDbRateLimitLabel}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding ProtonDbRateLimitMs}" Width="100"/>
                        </StackPanel>
                        <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_ProtonDbRateLimitDesc}" FontSize="11" Foreground="Gray" Margin="0,0,0,8"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_SteamStoreRateLimitLabel}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding SteamStoreRateLimitMs}" Width="100"/>
                        </StackPanel>
                        <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_SteamStoreRateLimitDesc}" FontSize="11" Foreground="Gray" Margin="0,0,0,16"/>

                        <!-- Debug / Progress -->
                        <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_DebugHeader}" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_DebugProgressDelay}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding DebugProgressDelayMs}" Width="100"/>
                        </StackPanel>
                        <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_DebugProgressDelayDesc}" FontSize="11" Foreground="Gray" Margin="0,0,0,16"/>
                        
                        <!-- ProtonDB API URL -->
                        <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_ApiSettings}" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>
                        <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_ApiUrl}" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding ProtonDbApiUrl}" Margin="0,0,0,8" TextWrapping="Wrap"/>
                        <TextBlock Text="{DynamicResource LOC_SteamDeckProtonDb_ApiUrlDesc}" FontSize="11" Foreground="Gray"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>