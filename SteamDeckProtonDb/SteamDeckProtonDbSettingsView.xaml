<UserControl x:Class="SteamDeckProtonDb.SteamDeckProtonDbSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="400">
    <Grid Margin="12">
        <StackPanel>
            <TextBlock Text="Steam Deck Metadata" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>
            <CheckBox IsChecked="{Binding EnableSteamDeckCategories}" Margin="0,0,0,8">
                <TextBlock Text="Enable Steam Deck Compatibility Categories"/>
            </CheckBox>
            <TextBlock Text="Adds Steam Deck compatibility status as categories." FontSize="11" Foreground="Gray"/>
            
            <!-- ProtonDB Settings -->
            <TextBlock Text="ProtonDB Metadata" FontWeight="Bold" FontSize="14" Margin="12,16,0,8"/>
            <CheckBox IsChecked="{Binding EnableProtonDbCategories}" Margin="0,0,0,8">
                <TextBlock Text="Enable ProtonDB Tier Categories"/>
            </CheckBox>
            <CheckBox IsChecked="{Binding EnableProtonDbLink}" Margin="0,0,0,8">
                <TextBlock Text="Enable ProtonDB Link in game details"/>
            </CheckBox>
            <TextBlock Text="Adds ProtonDB tier categories and optional link to ProtonDB page." FontSize="11" Foreground="Gray"/>
            
            <!-- Tags Settings -->
            <TextBlock Text="Tags" FontWeight="Bold" FontSize="14" Margin="12,16,0,8"/>
            <CheckBox IsChecked="{Binding EnableTags}" Margin="0,0,0,8">
                <TextBlock Text="Enable Tags"/>
            </CheckBox>
            <TextBlock Text="Adds tags for compatibility status (e.g., steamdeck:verified, protondb:platinum)." FontSize="11" Foreground="Gray" Margin="0,0,0,12"/>
            
            <!-- Cache Settings -->
            <TextBlock Text="Cache Settings" FontWeight="Bold" FontSize="14" Margin="12,16,0,8"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="Cache TTL (minutes):" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox Text="{Binding CacheTtlMinutes}" Width="100"/>
            </StackPanel>
            <TextBlock Text="How long to cache metadata before fetching fresh data. Default: 1440 (24 hours)." FontSize="11" Foreground="Gray"/>
            
            <!-- ProtonDB API URL -->
            <TextBlock Text="ProtonDB API Configuration" FontWeight="Bold" FontSize="14" Margin="12,16,0,8"/>
            <TextBlock Text="ProtonDB API URL:" Margin="0,0,0,5"/>
            <TextBox Text="{Binding ProtonDbApiUrl}" Margin="0,0,0,8" TextWrapping="Wrap"/>
            <TextBlock Text="API endpoint for fetching ProtonDB data. Use {0} as placeholder for AppId." FontSize="11" Foreground="Gray"/>
        </StackPanel>
    </Grid>
</UserControl>